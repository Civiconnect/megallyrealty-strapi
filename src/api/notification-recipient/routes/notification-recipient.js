'use strict';

/**
 * notification-recipient router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::notification-recipient.notification-recipient');
